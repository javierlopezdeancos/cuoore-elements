{
  "name": "cuoore-elements",
  "description": "Native custom elements independently deployable ready to browser consume",
  "autor": "Javier Lopez de Ancos",
  "version": "0.1.0",
  "license": "gpl-3.0",
  "scripts": {
    "start": "npx nx start server",
    "build": "npx nx run-many --target=build",
    "build:no-cache": "npx nx run-many --target=build --skip-nx-cache",
    "lint": "npx nx lint",
    "graph": "npx nx graph"
  },
  "private": false,
  "devDependencies": {
    "nx": "15.5.1",
    "prettier": "^2.6.2",
    "typescript": "^4.9.4",
    "vite": "^4.0.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
